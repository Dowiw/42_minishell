{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Minishell: Attach inside WSL",
            "type": "cppdbg",
            "request": "attach",
            "program": "${workspaceFolder}/minishell",
            "processId": "${command:pickProcess}",
            "MIMode": "gdb"
        }
    ]
}
